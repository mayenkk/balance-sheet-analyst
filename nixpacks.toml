[phases.setup]
nixPkgs = ["conda", "nodejs_20", "npm", "curl"]

[phases.install]
cmds = [
  "chmod +x build.sh",
  "./build.sh"
]

[phases.build]
cmds = [
  "source activate analyst-env"
]

[start]
cmd = "source activate analyst-env && uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT" 