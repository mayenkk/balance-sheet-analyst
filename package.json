{
  "name": "balance-sheet-analyst",
  "version": "1.0.0",
  "description": "AI-powered balance sheet analysis platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p backend/static && cp -r frontend/build/* backend/static/",
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
} 